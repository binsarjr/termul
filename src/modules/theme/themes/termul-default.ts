import type { Theme } from "../types";

export const termulDefault: Theme = {
  id: "termul-default",
  name: "Termul Default",
  description: "The default Termul look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
