import type { Theme } from "../types";

export const ijtDefault: Theme = {
  id: "ijt-default",
  name: "Its Just Terminal Default",
  description: "The default Its Just Terminal look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
